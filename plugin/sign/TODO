* glue, delegation
* go-routine, per zone to sign, stop and reload etc?
* check if we need to resign, set period to sign in time.Ticker?
* more tests, test nsec sorting, test zone signing apex only zone etc.
* need sorted types out of e.Elem.Types()
// resign_test.go
